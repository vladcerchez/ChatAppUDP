
---- Deschide-ti proiectul (IntelliJ) și rulați metoda main din << ChatServer class >> ----
     Apoi rulați tot ca proiect << gradle >> iar pentru mai mulți clienți, în con-
     figurări la gradle bifați << allow multiple instances >>
     Pentru mesaj privat folosiți comanda << /PS nume_client mesaj >>

---- Open the project (IntelliJ) and run the main method from the << ChatServer class >> ----
     Then run everything as a << gradle >> project and for multiple clients, in the gradle configurations, check << allow multiple instances >>.
     For private messages, use the command << /PS client_name message >>.